packages:
  # EL compat
  - dnf-yum
  - dnf-bootc

postprocess:
  - |
    #!/usr/bin/env bash
    mkdir -p /usr/lib/bootc/install/
    cat > /usr/lib/bootc/install/20-rhel.toml << EOF
    [install]
    root-fs-type = "xfs"
    EOF

include:
  - standard/manifest.yaml
